server:
  port: ${PORT:8082}

spring:
  data:
    cassandra:
      port: 24000
      username: test
      password: test
      cluster-name: thoughtfulness-pear-kyi8
      contact-points: 35.247.31.132

eureka:
  client:
    registerWithEureka: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
    healthcheck:
      enabled: true

info:
  app:
    name: ${spring.application.name}

management:
  endpoints:
    web:
      exposure:
        include:
          - mappings
          - env
          - health
          - info
